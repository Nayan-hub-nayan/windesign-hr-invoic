# Google OAuth — replace with your real credentials from Google Cloud Console
# https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Optional: restrict sign-in to this email domain (leave empty to allow any Google account)
VITE_ALLOWED_EMAIL_DOMAIN=windesign.io
